name: check-availability
on: [push]

jobs:
  check-availability-on-ubuntu:
    name: Check availability
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
      - run: npm help

